# 审查回执：iFinD 双源验证设计变更（候选池验证）+ 实盘组合胜率待补

> 对应：K3 查实（2026-08-20，Kimi Work 会话）
> **变更：双源交叉验证从"三年全量导出"改为"候选池验证"——K3 查实插件通道做不了全量**

---

## 一、iFinD 两层通道查实结果

| 通道 | 状态 | 说明 |
|---|---|---|
| Kimi 插件通道 | ✅ **已通** | 实测取到浦发银行（600000.SH）08-20 真实行情（收盘 9.11 元），数据可用 |
| 用户自有账号（iFinDPy） | ⚠️ 无法从工作区探测 | quant venv 未装 iFinDPy、.env 无 iFinD 凭据、导出脚本在 Kimi chat 侧环境——通道状态需在原环境测 |

## 二、设计变更：全量导出 → 候选池验证

**K3 查实的关键限制**：插件全部 9 个接口均为单票/少票导向（每次 ≤3 只、单查询最长 3 年、无全市场 daily_basic 式截面接口）。
→ 5500 只 × 182 周全市场截面需要**数千次调用**，全量导出不现实。

**新设计（双方接受）**：
1. **候选池验证**（主）：只对组合实际持仓 + Top50 候选做双源核对（因子值/IC），验证 v3 五因子框架在 iFinD 源下的一致性
2. **抽样验证**（备）：每周截面随机抽几百只算 RankIC/十分组（若需全市场级统计量）
3. **附加价值**：顺带验证 8 只实盘持仓在 v3 五因子下的得分排名 → 看实盘组合与新体系重合度

## 三、实盘模型组合胜率——待用户提供持仓

- K3 侧：工作区只有业绩数字（07-24 建仓至 08-14 +1.93%），**无持仓明细**（记录在 Kimi chat 侧环境）
- 待用户提供：8 只代码 + 实际买入价（可选，默认 07-24 收盘近似）
- 双源对照：Hermes（Tushare 行情）+ K3（iFinD 插件）各出一份逐只盈亏/胜率/组合收益对照表

## 四、状态

- [x] iFinD 通道查实（K3）
- [x] 双源验证设计变更落定（候选池验证）
- [ ] 用户提供 8 只持仓代码 → 双源出胜率对照表
- [ ] 候选池验证执行（iFinD 侧 K3 / Tushare 侧 Hermes）
